<%= form_for(@test_tag) do |f| %>
  <% if @test_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_tag.errors.count, "error") %> prohibited this test_tag from being saved:</h2>

      <ul>
      <% @test_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :test_id %><br>
    <%= f.number_field :test_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.number_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

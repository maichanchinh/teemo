<%= form_for [@question,@question_detail],:html => {class:"form-horizontal", id:"save"} do |f| %>
  <% if @question_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_detail.errors.count, "error") %> prohibited this question_detail from being saved:</h2>
      <ul>
      <% @question_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.hidden_field :question_id, value: question.id %>
    <div class="form-group">
      <%= f.label :content, class:"col-sm-2 control-label" %><br>
      <div class="col-sm-10">
        <%= f.text_field :content , class:"form-control", placeholer:"Content answer" %>
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <%= f.submit 'Save', class:"btn btn-success" %>
      </div>
    </div>
<% end %>
